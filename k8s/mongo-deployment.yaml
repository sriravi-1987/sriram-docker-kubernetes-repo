apiVersion: apps/v1
kind: Deployment
metadata:
  name: shopsphere-mongo
  namespace: shopsphere
spec:
  replicas: 1
  selector:
    matchLabels:
      app: shopsphere-mongo
  template:
    metadata:
      labels:
        app: shopsphere-mongo
    spec:
      containers:
        - name: mongo
          image: mongo:7.0
          ports:
            - containerPort: 27017
          volumeMounts:
            - name: mongo-data
              mountPath: /data/db
      volumes:
        - name: mongo-data
          emptyDir: {}